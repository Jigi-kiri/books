export interface LoginCredProps {
    email: string;
    password: string;
}

export interface BookProps {
    id: number;
    title: string;
    author: string;
    publicationYear: string;
    gener: string;
}